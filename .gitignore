# Private business documents — never commit
CLIENT-CONTRACT.md

# Claude internal agent worktrees
.claude/worktrees/

# Media files (large raw assets — exceptions below for committed web assets)
*.mp4
*.mov
*.avi

# Allow the compressed hero video in public/
!enchanted-madison/public/hero-forest.mp4

# Raw source photos from client (keep local; web-optimized versions live in /public)
source-photos/

# Playwright MCP audit output
.playwright-mcp/
