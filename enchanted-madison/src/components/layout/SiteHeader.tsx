// SiteHeader — transparent on hero, solid on scroll
// Phase 1 task: implement scroll behavior + mobile menu
// Design spec: design-system.md §5 (Navigation)
export function SiteHeader() {
  return <header>TODO: SiteHeader — Phase 1</header>;
}
