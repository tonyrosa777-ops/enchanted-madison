// /stays/[slug] — Individual Stay Page
// Phase 3 task: build per-property pages with Lodgify embed
// Source: initial-business-data.md §4, §9
export default function StayPage({ params }: { params: { slug: string } }) {
  return <div>TODO: Stay page for {params.slug} — Phase 3</div>;
}
